# sample_demo

main2mb.s  : Loads all samples into memory, requires a 2MB+ machine.
main2buf.s : Basic 2 buffer memory management of samples for 1MB machine. Some issues at the end of the example tune.
main3buf.s : 3 buffer memory management. Tested on real 1MB STe and plays the example tune properly.
